#include <iostream>
#include <string>
#include <list>
#include <algorithm>
using namespace std;

int main()
{
    list<int> list1;
    string a;

    cout << "Enter numbers (type 'stop' to finish):" << endl;

    while (true) {
        cin >> a;

        if (a == "stop") {
            break;
        }

        list1.push_back(stoi(a));
    }

    auto min_it = min_element(list1.begin(), list1.end());
    cout << *min_it << endl;

    system("PAUSE");
    return 0;
}
